#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
  * main - check the code
  * mul - this function multiplies two numbers
  * @a: the first number
  * @b: the second number
  * Return: Always 0
  */

int mul(int a, int b)
{
			int mul;

			mul = (a * b);
			return mul;
}
