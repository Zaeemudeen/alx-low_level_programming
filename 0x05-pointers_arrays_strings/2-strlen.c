#include <stdio.h>
#include "main.h"

/**
 * _strlen - function swaps two integers
 * @s: pointer to the string character
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int l;
	l = strlen(s);
}

