#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen_recursion - prints the length of s
 * @s: string
 * Return: length of s.
 */

int _strlen_recursion(char *s)
{
	return strlen(s);
}
